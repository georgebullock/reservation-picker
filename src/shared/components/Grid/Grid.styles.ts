import styled from "styled-components";

const Grid = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  max-width: 100%;
  margin-right: auto;
  margin-left: auto;
`;

export const Styled = {
  Grid,
};
